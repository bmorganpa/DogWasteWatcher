name: Spec

on:
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Run nextapp unit tests
      run: |
        cd nextapp
        npm install
        npm run spec
